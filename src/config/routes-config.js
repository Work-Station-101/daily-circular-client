const RouteUrls = {
  default: '/',
  base: '/',
  login: '/login',
  registration: '/register',
  timeline: '/timeline',
  createCircular: '/create-circular',
  myCirculars: '/my-circulars',
  profile: '/profile',
};

export default RouteUrls;
